# 历史文档归档

本目录只保存已被替代的旧审计和旧路线材料，用于追溯，不属于当前需求、路线图、契约或代码模板。

使用规则：

- 默认开发任务禁止读取本目录。
- 只有调查历史决策时才读取，并同时读取 `../product-scope.md`、`../roadmap.md` 和相关 accepted ADR。
- 归档内容中的“建议、阶段、方向正确”等措辞均是当时结论，不具有当前效力。
- 当前有效文档以 `../README.md` 的索引和优先级为准。
