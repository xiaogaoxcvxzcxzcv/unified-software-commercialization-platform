# Ordinary UI Template Catalog

Only UI template versions whose declared target, delivery mode and environment combinations are `ordinary + available` may be published here.

Layout:

```text
<template_id>/<version>/template.json
```

The directory is intentionally empty until a real template has runnable source, complete ready Feature Blocks, content-tree evidence and target E2E verification.
