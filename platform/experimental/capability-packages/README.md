# Controlled Experimental Capability Package Catalog

This server-controlled catalog accepts only `experimental + verified` combinations. It is never enabled by a client or admin-web request and is not visible in the ordinary software-creation flow.

Layout: `<package_id>/<version>/manifest.json`.
